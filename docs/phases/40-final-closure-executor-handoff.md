# Executor Handoff — Phase 40 Final Closure

Implement `docs/phases/40-final-closure-control-pack.md` completely on `codex/phase-40-final-remediation` from the exact master-owned starting commit supplied in the dispatch message.

Read the control pack, all protected MG tests, both Phase 40 verification scripts, migration `04600`, generation persistence, all Q/C/A/D/L/G/E1 matrix tests, the upgrade harness, and the current implementation report before editing.

Continue autonomously through independent expected-set construction, database closure/identity enforcement, exact matrix remediation, stable-path upgrade proof, focused tests, the mechanical pre-report stop, the full final gate, one implementation commit, and one report-only commit. Do not return progress, request “continue,” or stop for ordinary failures.

Do not edit protected master evidence, migrations `00100`–`04500`, create `04700`, rewrite history, push, merge, deploy, self-approve, start Phase 50, or change the selected executor model/reasoning settings.

Final status: `READY FOR INDEPENDENT PHASE 40 RE-REVIEW` or a genuine `BLOCKED` reason as defined by the control pack.
