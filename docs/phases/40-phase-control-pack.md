# Phase 40 Control Pack — Generation Engine

Status: **AUTHORIZED / IN PROGRESS**

## Identity and authority

- Repository: `C:\Users\Nadav\.codex\worktrees\2e66\Teaching-System-for-School-phase-20-git`
- Branch: `codex/phase-40-generation-engine`
- Starting commit: `fd419a5ef7577b6d2ca65ba6381a7a30e3200c18`
- Authority: Phase 30 approval was issued by the permanent master and recorded in `docs/phases/30-phase-review.md`.
- Dependent phase: Phase 50 is blocked until independent Phase 40 approval.

## Objective and exclusions

Implement a provider-neutral, deterministic Hebrew worksheet/test draft engine and isolated single-question regeneration. The engine consumes only Phase 30 eligible context, persists complete traceability, uses strict contracts, bounded budgets, trusted registries, idempotent outbox workflow, safe retries, and immutable assessment revisions.

Explicitly excluded: real provider SDKs, network/paid model calls, embeddings/vector/semantic/open-web retrieval, Phase 50 validation findings or overrides, editor/UI, PDF/DOCX/rendering, deployment, billing, students, collaboration, and role administration.

## Protected contracts

Preserve all seven prior migrations and their SHA-256 identities, Phase 10 outbox/audit/auth boundaries, persisted `resolveAccessContext`, Phase 20 published curriculum and immutable finalized revision/scoring contracts, and Phase 30 eligibility/retrieval/provenance/authorization/database invariants.

## Deliverables

| ID | Deliverable | Required evidence |
| --- | --- | --- |
| D1 | Strict v1 generation/regeneration/spec/output/gateway/status/context contracts and JSON Schemas | Runtime strict tests and generated byte-drift check |
| D2 | Provider-neutral gateway, immutable prompt/model registries, deterministic fake, fail-closed live preflight | Gateway outcome matrix and preflight PASS |
| D3 | GenerationRun, GenerationContextItem, GenerationUsage, QuestionSourceLink persistence | 04000 migration, direct DB matrix, append-only/identity checks |
| D4 | Internal authoritative eligible-context retrieval | Parameterized indexed query, bounded/deduplicated deterministic context tests |
| D5 | Four public operations and two internal operations | Persisted auth, mapping, idempotency, worker tests |
| D6 | Retry, timeout, budget, replay, crash/stale recovery | Gateway, retry, usage, terminal-state matrices |
| D7 | Isolated regeneration preserving unrelated revision graph | Ten-case regeneration matrix |
| D8 | Safe audit/outbox/log/error traceability | Redaction and append-only matrix |
| D9 | API/worker modular boundary | Architecture and provider-boundary tests |
| D10 | Runbook and deterministic Hebrew fixtures/evaluation | Exact fake evaluation results and runbook |

## Operation catalog

| Operation | Trusted input | Authorization | Result/failure |
| --- | --- | --- | --- |
| P1 `requestDraftGeneration` | Persisted tenant context plus strict draft plan | Active User, Membership, Organization; TEACHER/COORDINATOR/SCHOOL_ADMIN | Strict run summary; non-disclosing denial; idempotent retry/conflict |
| P2 `requestQuestionRegeneration` | Persisted assessment/base revision/target question plus strict instruction | Same persisted tenant policy and exact owned finalized base | Strict run summary; no client snapshot trust |
| P3 `getGenerationStatus` | Authenticated tenant context and run ID | Exact owner; missing/foreign returns null | Strict status |
| P4 `getGenerationResult` | Authenticated tenant context and run ID | Exact owner; missing/foreign returns null | Strict finalized revision/provenance or null |
| I1 `selectGenerationContext` | Trusted persisted run specification | Revalidates Phase 30 eligibility and owner | Bounded immutable context lineage or insufficient context |
| I2 `processGenerationRun` | Claimed ID-only outbox event | Worker-owned run lock and state transition | Exactly-once persisted terminal result |

## Run state policy

Only these transitions are valid: PENDING→PROCESSING; PROCESSING→PENDING; PROCESSING→SUCCEEDED; PROCESSING→INSUFFICIENT_CONTEXT; PROCESSING→FAILED. All other 25 ordered state pairs are permanently tested and database-enforced.

## Binding matrices

| Matrix | Required cases |
| --- | ---: |
| T tenant directions | 8 |
| R role happy path | 24 |
| S persisted-state denial | 24 |
| L run transitions | 25 |
| G gateway outcomes | 10 |
| E1 initial eligibility | 17 |
| E2 pre-commit invalidation | 7 |
| O strict output/adversarial | 12 |
| Q regeneration isolation | 10 |
| C concurrency/idempotency | 8 |
| A audit/redaction/append-only | 8 |
| D direct database adversarial | 20 |
| **Total** | **173** |

Every row executes with at least one case-result assertion, zero skips/todos/only, and actual counts recorded in the final report.

## Migration and mechanical gates

Create only `packages/db/prisma/migrations/20260824004000_phase40_generation_engine/migration.sql`; final migration count is 8/8. Verify all seven frozen migration SHA-256 values, fresh 8/8 databases, Phase 30→04000 upgrade, isolated shadow drift (`No difference detected`, `MIGRATION_DRIFT=PASS`), strict schema drift, no provider SDK/network/semantic/vector/open-web/Phase 50/editor/PDF/deployment/billing/student scope, authoritative context retrieval, row-lock revision allocation, redaction, and matrix manifest/runtime agreement.

## Final gate

Run frozen install, Prisma generate/validate/format plus exact schema diff, `pnpm verify:phase40`, format, lint, typecheck, unit/contract/architecture, deterministic fake evaluation, full PostgreSQL integration, Phase 40 upgrade, disabled live preflight, build, and `git diff --check`. All required tests have zero skips.

## Commit/report plan

1. Control/preparation commit: this pack, executor handoff, Phase 30 review artifact, project-state update.
2. One complete implementation commit with migration, production code, contracts/schemas, fixtures, tests, scripts, and runbook.
3. One report-only commit changing only `docs/phases/40-implementation-report.md`.

The report is a claim set, not approval. No push, merge, deploy, self-approval, live provider, or Phase 50 work is permitted.

## Genuine blocker

Only an unavoidable conflict with an approved Phase 20/30 contract, a required product decision that expands the approved local/provider-neutral scope, or an unavailable external dependency that cannot be replaced by the deterministic fake is a blocker. Ordinary implementation failures are not blockers.
