# Phase 40 Executor Handoff — Generation Engine

Implement the binding Phase 40 Control Pack completely on:

- Branch: `codex/phase-40-generation-engine`
- Starting commit: `fd419a5ef7577b6d2ca65ba6381a7a30e3200c18`

Read the repository governing documents, Phase 20/30 contracts and reports, Prisma schema and migrations, all runbooks, packages/ai, packages/contracts, packages/domain, packages/db, apps/api, apps/worker, architecture tests, CI, and package scripts completely before editing.

Preserve all Phase 10/20/30 interfaces, migrations, eligibility, authorization, outbox, scoring, and immutable revision behavior. Implement only provider-neutral deterministic generation and isolated question regeneration. Do not install or call a real provider, use network model calls, add embeddings/semantic/open-web retrieval, start Phase 50, add editor/PDF/deployment/billing/student/collaboration features, or weaken any protected contract.

Continue autonomously through control documentation, strict contracts and generated schemas, migration and direct-database invariants, trusted retrieval/context, gateway and registries, persistence, public operations, worker/retry/recovery, regeneration, all 173 matrix cases, mechanical stop, full final gate, implementation commit, report, and report-only commit. Do not return a progress-only response or ask for continuation.

Before the report, `pnpm verify:phase40` and every final gate must pass. The live evaluation must remain fail-closed and the report must state exactly: `LIVE PROVIDER EVALUATION NOT RUN — OWNER APPROVAL AND PROVIDER DECISION REQUIRED`.

Create exactly three commits: control/preparation, implementation, report-only. Do not amend, rewrite, push, merge, deploy, self-approve, or start Phase 50.

Final status is exactly `READY FOR INDEPENDENT PHASE 40 REVIEW` or `BLOCKED`.
